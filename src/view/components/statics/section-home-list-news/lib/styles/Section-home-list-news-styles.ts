import styled from 'styled-components';

const SectionHomeListNews = styled.section``;

const Wrap = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32px;
`;

const Title = styled.h2`
  font: var(--h1);
  color: var(--black);
`;

export { SectionHomeListNews, Wrap, Title };
