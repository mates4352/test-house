import styled from 'styled-components';

const SectionContacts = styled.div``;

const Wrap = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 32px;
`;

const Title = styled.h2`
  font: var(--h1);
  color: var(--black);
`;

export { SectionContacts, Wrap, Title };
