import styled from "styled-components";

const Contacts = styled.div`

`

const List = styled.ul`
  display: grid;
  grid-auto-rows: minmax(min-content, max-content);
  row-gap: 24px;
`

const Item = styled.li`
  
`

export {Contacts, List, Item}