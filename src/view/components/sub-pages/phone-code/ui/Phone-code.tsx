import React, { FC } from 'react';
import { FormPhoneCode } from '../../../forms/form-phone-code';

type PhoneCodeType = {};

export const PhoneCode: FC<PhoneCodeType> = ({}) => {
  return <FormPhoneCode />;
};
